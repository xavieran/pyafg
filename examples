#Remember, remove the -o to view the fractal in pygame
#Draws Barnsley's Fern
pyafg.py -s 100 -o fern.png fractals/fern.frct
#Draws it very big. Note how we've used the offsets to zoom in on
#a specific portion of it.
pyafg.py -s 1000 -o fernbig.png -g 800x600 -x -880 -y -500 -i 2000000 fractals/fern.frct
#Draw the fern using colors from the colors.txt file.
pyafg.py -s 100 -o fern_colored.png --color-type 1 --color-file colors.txt fractals/fern.frct
